Eres el worker de implementación para el repo `seguridad_publica` (Next.js 16 + React 19 + Tailwind v4, `DESIGN.md` en la raíz es la fuente única de verdad visual del proyecto, deploy en Vercel).

Hay un plan ya diseñado en la carpeta `plan-apple-pilot/` (raíz del repo). Tu trabajo es **ejecutarlo etapa por etapa**, no rediseñarlo.

Es un piloto acotado: el usuario pidió un lenguaje visual "premium, estilo Apple" para Centinela. Se le advirtió que el sistema es en su mayoría dashboards/formularios/tablas (no el terreno de ese estilo) y decidió limitar el alcance a **solo 2 pantallas**: Login (`app/(auth)/login/`) y el Hub de módulos (`app/dashboard/page.tsx` + `app/dashboard/module-cards.tsx`), sin modo oscuro. **Ninguna otra vista del sistema se toca.**

Instrucciones:

1. Lee `plan-apple-pilot/README.md` completo primero.
2. Lee `plan-apple-pilot/00-contexto.md` completo — ahí está el estado real del código ya verificado (login usa una hoja de estilos bespoke que no pasa por `DESIGN.md`, `--red`/`--gold` ya son `#1f355a` en la práctica, `DashboardHeader` es un componente compartido en ~100 páginas, `ModuleCards` ya tiene una composición tipo bento) y las decisiones de diseño ya tomadas (tipografía de sistema, un solo acento de color, materialidad "glass", motion con `framer-motion`, iconos `lucide-react`) — no las reabras ni las cuestiones.
3. Antes de escribir código, lee con tus propios ojos: `DESIGN.md`, `app/globals.css` (bloque `@theme inline`), `app/(auth)/login/page.tsx`, `app/(auth)/login/login.css`, `app/dashboard/page.tsx`, `app/dashboard/module-cards.tsx`, `components/partials/Header.tsx`. El plan describe su estado a la fecha de diseño; si algo cambió, prioriza el código real, mantén el mismo objetivo/patrón de la etapa, y avísalo explícitamente en tu reporte.
4. Ejecuta `etapa-1.md`. Al terminar, corre sus "Criterios de aceptación" (incluye `npx tsc --noEmit` y `npm run build`). Reporta qué hiciste y **detente** — no sigas a la Etapa 2 sin que el usuario confirme.
5. Repite el mismo patrón para `etapa-2.md` → `etapa-5.md`, siempre deteniéndote al final de cada una. Las etapas son secuenciales y dependen de la anterior (Etapa 1 crea los tokens que consumen las Etapas 3-5; Etapa 2 crea el `variant` que usa la Etapa 4) — no las combines ni adelantes trabajo.
6. En la Etapa 2, el prop `variant` de `DashboardHeader` debe tener default `'tactico'` — verifica con un grep (`grep -rn "<DashboardHeader" app/ components/`) que ninguna llamada existente empieza a pasar `variant` por accidente. Las ~100 páginas que usan este header deben quedar pixel-idénticas a como estaban.
7. En las Etapas 3-5, no toques lógica: en el login, el flujo `idle → submitting-1 → otp → submitting-2 → success`, la validación, `authClient.signIn.email`/`authClient.twoFactor.verifyTotp` y el redirect a `fromPath` quedan exactamente igual — solo cambia CSS y algunas literales de texto en mayúsculas (ver la tabla de la Etapa 3). En el dashboard, no toques el redirect por rol (`obtenerHubRol`) ni el gate de `esAdmin` para `SspmGeneral`. En `ModuleCards`, no toques el array `MODULES` (labels, `href`, `size`, `status`, `stats`) ni la composición del grid.
8. No agregues dependencias nuevas al `package.json` — todo se construye con lo ya instalado (`framer-motion`, `lucide-react`, Tailwind v4). No agregues fuentes nuevas (`@font-face`/Google Fonts) — la tipografía Apple-style usa la pila de sistema (`-apple-system, BlinkMacSystemFont, ...`), definida como token en la Etapa 1.
9. No toques ninguna vista fuera de las 5 etapas (911, Fiscalía, Juzgado, Flota, formularios, tablas, catálogos, admin, PWA offline, etc.) — es fuera de alcance explícito, ver la sección correspondiente en `README.md`.
10. Antes de cualquier cambio de UI, lee `DESIGN.md` completo (incluida la sección `## 10 · Piloto Apple-Style` que agrega la Etapa 1) — sigue siendo la fuente de verdad visual única del proyecto.
11. Al terminar la Etapa 5, corre el checklist general de `README.md` (build completo, verificación manual de las 2 pantallas en los 3 breakpoints — móvil ≤720px, tablet 721-1200px, desktop >1200px — y sanity check de que ninguna otra página cambió) y agrega el ADR en `boveda/🏗 Arquitectura/Decisiones.md`.

Empieza confirmando que leíste `README.md` y `00-contexto.md`, y luego arranca la Etapa 1.
