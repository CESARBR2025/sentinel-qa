Eres el worker de implementación para el repo `seguridad_publica` (Next.js 16 + Postgres, capa `lib/<modulo>/{types,repository,service,actions}.ts`, sin ORM salvo better-auth con Drizzle, migraciones SQL manuales en `lib/db/manual-migrations/`, deploy en servidor Node persistente vía `next start`, no serverless).

Hay un plan ya diseñado en la carpeta `plan-pwa-push/` (raíz del repo). Tu trabajo es **ejecutarlo etapa por etapa**, no rediseñarlo. Cubre dos entregables relacionados: (1) notificaciones push reales a dispositivo (celular/laptop) usando Web Push estándar con VAPID — no Firebase — enganchadas al sistema de notificaciones in-app que ya existe; y (2) mejoras de instalabilidad de la PWA (iconos maskable + prompt de instalación), necesarias porque en iPhone/iPad el push solo funciona si la app está instalada al Home Screen.

El proyecto **ya tiene** una base de PWA (manifest + service worker manual en `public/sw.js`, solo para fallback offline) y un sistema de notificaciones in-app maduro (`lib/notificaciones/`, con `emisor.ts` como único punto de fan-out a roles/usuarios). Este plan **reutiliza ambas piezas al máximo** — el push se agrega como 2 listeners nuevos al `sw.js` existente (sin tocar su lógica offline), y el enganche con la lógica de negocio real es **un solo cambio** dentro de `emisor.ts`, no se toca ninguno de los ~10 archivos que ya llaman a `emitir()` (despacho 911, fiscalía, juzgado, etc.). Todo esto está detallado en `00-contexto.md`, no lo reinventes.

Instrucciones:

1. Lee `plan-pwa-push/README.md` completo primero.
2. Lee `plan-pwa-push/00-contexto.md` completo — ahí está el estado real del proyecto (qué ya existe vs. qué falta), la decisión de arquitectura (Web Push/VAPID sobre Firebase, y por qué), el diseño de la tabla `push_subscriptions`, y las 7 decisiones que ya tomó el arquitecto — no las vuelvas a preguntar.
3. Antes de escribir código, revisa con tus propios ojos los archivos existentes que este plan reutiliza y extiende: `public/sw.js`, `public/manifest.json`, `components/sw-register.tsx`, `app/layout.tsx`, `lib/notificaciones/emisor.ts`, `lib/notificaciones/actions.ts` (patrón `sesionConRol()`), `lib/notificaciones/catalogo.ts`, `components/notificaciones/CampanillaNotificaciones.tsx`, `lib/db.ts`, `lib/db/schema.ts` (tipo real de `users.id` y si existe `users.activo`), `lib/db/manual-migrations/README.md` y el precedente de migración más reciente. El plan describe su estado al 2026-08-05; si algo cambió, prioriza lo que encuentres y avísalo.
4. Ejecuta `etapa-1.md`. Al terminar, corre sus "Criterios de aceptación". Reporta qué hiciste y **detente** — no sigas a la Etapa 2 sin que el usuario confirme.
5. Repite el mismo patrón para `etapa-2.md` → `etapa-7.md`, siempre deteniéndote al final de cada una.
6. Si en cualquier etapa el código real no coincide con lo que describe el plan (tipo de columna, estructura de un archivo, nombre de un componente), prioriza lo real, ajusta manteniendo el mismo patrón, y avísalo explícitamente en tu reporte — no lo resuelvas en silencio.
7. No toques ningún archivo que no esté listado explícitamente en la etapa que estás ejecutando — en particular, nada de `lib/incidentes/`, `lib/fiscalia/`, `lib/agente_juzgado/`, `lib/notificaciones/checker.ts` ni `lib/notificaciones/admin-actions.ts` (el enganche de push es enteramente dentro de `lib/notificaciones/emisor.ts`, Etapa 5), y nada de la lógica offline existente de `public/sw.js` (`install`/`activate`/`fetch` se quedan intactos, Etapa 3 solo agrega 2 listeners). Si crees que hace falta tocar algo fuera de esa lista, pregunta antes.
8. Antes de cualquier cambio de UI (Etapas 4 y 6), lee `DESIGN.md` (raíz del repo) — el toggle de push y el banner de instalación deben verse consistentes con el resto de la app, no inventar un estilo nuevo.
9. La migración de la Etapa 1 sigue la convención de `lib/db/manual-migrations/README.md` — verifica el siguiente número libre antes de nombrar el archivo, no asumas que sigue siendo `0040`.
10. Las llaves VAPID generadas en la Etapa 1 y cualquier valor de `.env` son secretos reales — no los imprimas completos en tu reporte de la etapa, confirma solo que existen y que el formato es el esperado.
11. En la Etapa 5, presta atención especial a la nota sobre `ON CONFLICT ... DO NOTHING` y `dedup`: el push solo debe dispararse cuando la fila de `notificaciones_eventos` se insertó de verdad (usar `RETURNING id`), no en cada intento aunque haya sido deduplicado.

Empieza confirmando que leíste `README.md` y `00-contexto.md`, y luego arranca la Etapa 1.
