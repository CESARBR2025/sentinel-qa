Eres el worker de implementación para el repo `seguridad_publica` (Next.js + Postgres, capa `lib/<modulo>/{types,repository,service,actions}.ts`, sin ORM salvo auth, migraciones SQL manuales en `lib/db/manual-migrations/`).

Hay un plan ya diseñado y verificado varias veces contra la BD real (conexión directa por `pg`/`DATABASE_URL`) y el código real, en la carpeta `plan-formato-incidencias/` (raíz del repo). Tu trabajo es **ejecutarlo etapa por etapa**, no rediseñarlo.

Este plan reutiliza el módulo `lib/formatos-udai/` que ya existe (de `plan-formato-faltasadministrativas/`, ya implementado) — pero **NO usa `iph_detenidos`**. El ancla del reporte es `incidentes` (911), y la cadena real es 911 → reporte de campo del oficial (`ofi_reportes_campo`) → reporte de denuncia (`ofi_reporte_denuncia`), filtrada a incidentes ya resueltos (`estatus IN ('atendido','cerrado_detencion')`). Esto se decidió tras 3 revisiones del plan — no reintroduzcas `iph_detenidos` ni preguntes si debería usarse, ya se investigó y se descartó explícitamente (ver `00-contexto.md`, sección de revisión 3).

El corazón del plan sigue siendo el **paso de captura manual**: columnas sin ninguna fuente en `incidentes`/`ofi_reportes_campo`/`ofi_reporte_denuncia`/`ofi_detalles_asegurados` (`RT`, `TURNO`, `ARTICULOS U OBJETOS`, `AP/NUC`, `FOLIO RND`, `AGRUPAMIENTO`, `OTRO DELITO`, `MASC`, `UMECAS`, y varios campos del detenido si Análisis no los capturó) se llenan con un formulario por registro que, al guardarse marcando "completa", habilita ese registro para exportar. La tabla se ve segmentada `Pendientes`/`Completas` — el Excel solo exporta los "Completas".

Instrucciones:

1. Lee `plan-formato-incidencias/README.md` completo primero — en particular la sección "Cómo llegó a esta forma", que explica por qué se descartó `iph_detenidos`.
2. Lee `plan-formato-incidencias/00-contexto.md` completo — el mapeo columna por columna de ambas hojas contra la cadena 911 real, distinguiendo qué se resuelve 100% automático, qué viene del JSON sin esquema fijo de `ofi_reportes_campo`, y qué necesita el formulario de captura manual. También están las decisiones que ya tomó el usuario — no las vuelvas a preguntar.
3. Antes de escribir código, revisa con tus propios ojos: `lib/formatos-udai/types.ts`, `lib/formatos-udai/repository.ts`, `lib/formatos-udai/permisos.ts`, `app/formatos-udai/page.tsx`, `app/formatos-udai/faltas-administrativas/page.tsx`, `app/api/formatos-udai/faltas-administrativas/exportar/route.ts`, `components/formatos-udai/BotonExportarExcel.tsx`, `components/formatos-udai/DetalleFaltaAdministrativaModal.tsx`, `components/partials/SegmentPage.tsx`, `lib/db/manual-migrations/0038_antecedentes_externos_detenido.sql` y su `README.md`, y `lib/incidentes/actions.ts` (para confirmar la máquina de estados `ESTATUS_INCIDENTE`). El plan describe su estado al 2026-08-05; si algo cambió, prioriza lo que encuentres.
4. Ejecuta `etapa-1.md`. Al terminar, corre sus "Criterios de aceptación". Reporta qué hiciste y **detente** — no sigas a la Etapa 2 sin que el usuario confirme.
5. Repite el mismo patrón para `etapa-2.md` → `etapa-6.md`, siempre deteniéndote al final de cada una.
6. Si en cualquier etapa el código o la BD real no coinciden con lo que describe el plan, prioriza lo real, ajusta manteniendo el mismo patrón, y avísalo explícitamente en tu reporte — no lo resuelvas en silencio. Presta especial atención al esquema de `users` (nombre real de las columnas `name`/`apellido`) y a las llaves reales presentes en `ofi_reportes_campo.ofi_vehiculos`/`ofi_detenidos` (JSON sin esquema fijo, verificado con solo 2 muestras reales — puede haber más variantes).
7. No toques ningún archivo que no esté listado explícitamente en la etapa que estás ejecutando — en particular, nada de "Faltas Administrativas" ni del módulo de Análisis (`formAnalisis.tsx`, `useAnalistaForm.ts`, `iph_detenidos`). Si crees que hace falta tocar algo fuera de esa lista, pregunta antes.
8. Antes de cualquier cambio de UI, lee `DESIGN.md` (raíz del repo).
9. Los encabezados del Excel exportado (Etapa 5) se copian carácter por carácter del archivo oficial `FORMATO INCIDENCIA.xlsx` — no los corrijas ni normalices.
10. La migración de la Etapa 1 sigue la convención de `lib/db/manual-migrations/README.md` — verifica el siguiente número libre antes de nombrar el archivo, no asumas que sigue siendo `0039`.

Empieza confirmando que leíste `README.md` y `00-contexto.md`, y luego arranca la Etapa 1.
