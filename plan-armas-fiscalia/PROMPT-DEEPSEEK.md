Eres el worker de implementación para el repo `seguridad_publica` (Next.js 16 + Postgres, capa `lib/<modulo>/{types,mapper,repository,service,actions}.ts`, sin ORM salvo better-auth con Drizzle, migraciones SQL manuales en `lib/db/manual-migrations/`).

Hay un plan ya diseñado en la carpeta `plan-armas-fiscalia/` (raíz del repo). Tu trabajo es **ejecutarlo etapa por etapa**, no rediseñarlo. Construye la infraestructura para que Fiscalía capture tipo/marca/matrícula/calibre de armas aseguradas (dato que hoy no existe estructurado en ningún lado del sistema, verificado contra la BD real) y se sincronice automáticamente al paso 7 ("Armas de Fuego Aseguradas") del stepper de Formato N — hoy 100% manual.

El plan **reutiliza al máximo dos patrones que ya existen en el código**, no los reinventes:
1. El módulo `AntecedenteExterno` de `lib/fiscalia/` (tipos/mapper/repository/service/actions + componente `AntecedentesExternos.tsx`) — es el gemelo exacto de lo que hay que construir para "Armas Aseguradas": lista repetible por `reporte_campo_id`, capturada por Fiscalía, alta/baja simple.
2. El patrón `origen_*_id` + `INSERT ... ON CONFLICT DO UPDATE` que ya sincroniza Eventos (paso 1) y RND (paso 4) de Formato N (`lib/reportes/formato-n-eventos-service.ts`, `formato-n-rnd-service.ts`) — construido en la sesión inmediatamente anterior a este plan, incluyendo un bug real de orden de limpieza de duplicados ya corregido ahí (huérfanos-por-origen antes que duplicados-exactos). Replica el mismo orden, no lo reinventes ni lo invierta.

Todo esto está detallado con archivos y números de línea exactos en `00-contexto.md` — no lo vuelvas a investigar desde cero.

Instrucciones:

1. Lee `plan-armas-fiscalia/README.md` completo primero.
2. Lee `plan-armas-fiscalia/00-contexto.md` completo — ahí está el diagnóstico verificado contra la BD real (por qué no hay fuente estructurada hoy), la decisión de negocio ya confirmada con el usuario (captura en Fiscalía, no en campo; varias armas por reporte), y los dos patrones exactos a replicar.
3. Antes de escribir código, revisa con tus propios ojos los archivos que el plan extiende y clona: `lib/fiscalia/types.ts`, `mapper.ts`, `repository.ts` (función `listarAntecedentesExternos` y vecinas), `service.ts`, `actions.ts`; `components/fiscalia/AntecedentesExternos.tsx` y su montaje en `components/fiscalia/FormularioAsegurado.tsx`; `lib/reportes/formato-n-eventos-service.ts` y `formato-n-rnd-service.ts` completos (el patrón de upsert + limpieza de huérfanos); `lib/reportes/formato-n-armas-aseguradas-service.ts` actual (incluye código muerto, `obtenerArmasParaFormatoN`, que la Etapa 4 elimina); `app/envio-de-formatos/reporte/[fecha]/page.tsx` (`PasoArmas`, `PasoEventos`, `PasoRnd`). El plan describe su estado al cierre de la sesión que lo diseñó; si algo cambió, prioriza lo real y avísalo.
4. Ejecuta `etapa-1.md`. Al terminar, corre sus "Criterios de aceptación". Reporta qué hiciste y **detente** — no sigas a la Etapa 2 sin que el usuario confirme.
5. Repite el mismo patrón para `etapa-2.md` → `etapa-6.md`, siempre deteniéndote al final de cada una.
6. Si en cualquier etapa el código real no coincide con lo que describe el plan (nombre de columna, estructura de un archivo, número de línea desactualizado), prioriza lo real, ajusta manteniendo el mismo patrón, y avísalo explícitamente en tu reporte — no lo resuelvas en silencio. En particular, la Etapa 1 pide verificar contra la BD real si `evidencias_detenido`/`antecedentes_externos_detenido` tienen FK física antes de decidir si `fiscalia_armas_aseguradas` la lleva — no asumas la respuesta del plan sin correr esa query primero.
7. No toques ningún archivo que no esté listado explícitamente en la etapa que estás ejecutando — en particular, nada de `FotosExpedienteSection.tsx`, `app/fiscalia/solicitudes/[solicitudId]/page.tsx`, ni la lógica de cálculo de FGE/FGR/Eventos/RND. Si crees que hace falta tocar algo fuera de esa lista, pregunta antes.
8. Antes de cualquier cambio de UI (Etapas 3 y 5), lee `DESIGN.md` (raíz del repo) — el componente nuevo de Fiscalía y la tabla nueva del paso 7 deben verse consistentes con el resto de la app, no inventar un estilo nuevo. En la Etapa 3, copiar los estilos inline de `AntecedentesExternos.tsx` tal cual, no crear un sistema nuevo.
9. La migración de la Etapa 1 se aplica contra una BD real remota (`DATABASE_URL` no es localhost) — **confirma con el usuario antes de ejecutar cualquier DDL contra ella**, aunque sea aditivo (`CREATE TABLE`/`ADD COLUMN`/`CREATE INDEX`, no destructivo). Verifica el siguiente número libre de migración antes de nombrar el archivo (`ls lib/db/manual-migrations | sed -E 's/^([0-9]+)_.*/\1/' | sort -n | tail -1`), no asumas que sigue siendo `0047`.
10. Cualquier script tsx temporal que uses para verificar contra la BD real (Etapas 1, 2, 4) se borra del repo antes de cerrar esa etapa — no se commitea.
11. En la Etapa 4, presta atención especial al orden de `eliminarDuplicadosArmasDelDia`: el bloque que limpia huérfanos-por-`origen_fiscalia_arma_id` va **antes** que el bloque de duplicados exactos por campos — invertir el orden reproduce un bug real que ya pasó con Eventos en la sesión anterior (la limpieza genérica se queda con la fila vieja sin enlace en vez de la nueva enlazada).
12. Corre `npx tsc --noEmit` al final de cada etapa, `npm run build` completo al cerrar la Etapa 6, y `npx graphify update` antes de dar el plan por terminado.

Empieza confirmando que leíste `README.md` y `00-contexto.md`, y luego arranca la Etapa 1.
